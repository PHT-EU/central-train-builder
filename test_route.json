{
    "$id": "https://gitlab.com/PersonalHealthTrain/implementations/germanmii/common/schema/-/blob/master/Route.schema.json",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "title": "Test Route",
    "type": "object",
    "properties": {
      "harborProjects": {
        "type": "array",
        "description": "The list of harbor project names that are part of the route. Elements must be unique.",
        "uniqueItems": true,
        "items": {
          "type": "string",
          "contentMediaType": "text/plain"
        }
      },
      "repositorySuffix": {
        "type": "string",
        "description": "Repositories having this suffix are subject to the route.",
        "contentMediaType": "text/plain"
      },
      "periodic": {
        "type": "boolean",
        "description": "Whether the route repeats when exhausted",
        "default": false
      },
      "maxNumberOfStops": {
        "type": "integer",
        "description": "The max number of stops this route can make. If missing, the maximum number of stops is unlimited.",
        "minimum": 1
      }
    },
    "required": [
      "harborProjects",
      "repositorySuffix"
    ]
}

